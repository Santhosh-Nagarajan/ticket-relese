name: Merge Feature Branch

on:
  push:
    branches:
      - ahamed

jobs:
  merge-feature:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Git User
      run: |
        git config user.name "Santhosh-Nagarajan"
        git config user.email "nsmgbros001@gmail.com"

    - name: Merge Feature Branch
      run: |
        git remote -v
        git branch

        git pull
        git branch --all
        git checkout develop
        git branch
        git pull origin develop --allow-unrelated-histories
        
        git status
        git add .
        git commit -m "new" 
        
        
        git rebase develop ahamed
        git rebase ahamed develop
        git log
        git remote set-url origin https://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@github.com/Santhosh-Nagarajan/ticket-relese.git
        git remote -v
        git push -u origin develop
        
          

